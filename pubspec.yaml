name: netshelfy
description: Python Upload Server — bundled via Chaquopy
publish_to: 'none'
version: 1.0.3+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^11.3.1
  shared_preferences: ^2.2.3
  package_info_plus: ^8.0.0
  flutter_foreground_task: ^8.0.1   # background service
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
